enabled: true
script_type: prefect
description: "Simple Prefect pipeline for testing graph asset mapping"
group: test_examples
tags:
  type: test
  demonstrates: prefect_graph_mapping
kinds:
  - python
  - prefect

# Enable Prefect → Dagster mapping
prefect_mapping:
  enabled: true
  fallback_on_error: true
  mode: "graph_asset"
